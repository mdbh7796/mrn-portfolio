PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/mrn_portfolio
CLIENT_URL=http://localhost:5173

# Contact email notifications (Resend). Optional: when unset, messages are
# only saved to MongoDB. Set these in Vercel > Settings > Environment Variables too.
RESEND_API_KEY=
CONTACT_TO=you@example.com
# CONTACT_FROM=Portfolio <onboarding@resend.com>
